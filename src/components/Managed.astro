---
---

<section class="managed" id="managed">
  <div class="container">
    <div class="managed-card">
      <div class="managed-glow"></div>
      <div class="managed-content">
        <div class="managed-badge">Coming Soon</div>
        <h2 class="managed-title">Don't want to self-host?<br />We've got you.</h2>
        <p class="managed-desc">
          We're building a managed SubTrackr instance for folks who want the same
          privacy-respecting experience without maintaining their own server. Simple
          pricing, zero hassle.
        </p>
        <div class="managed-highlight">
          <div class="highlight-icon">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/></svg>
          </div>
          <p>
            A portion of every subscription goes directly to sponsoring maintainers
            of other self-hosted open source tools. Using SubTrackr means supporting
            the ecosystem that makes self-hosting great.
          </p>
        </div>
        <a href="https://github.com/bscott/subtrackr/discussions" target="_blank" rel="noopener" class="managed-cta">
          Join the Discussion
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="7" y1="17" x2="17" y2="7"/><polyline points="7 7 17 7 17 17"/></svg>
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .managed {
    padding: 4rem 0 8rem;
  }

  .managed-card {
    position: relative;
    background: var(--bg-card);
    border: 1px solid var(--border-subtle);
    border-radius: 24px;
    padding: 4rem;
    overflow: hidden;
    text-align: center;
    max-width: 720px;
    margin: 0 auto;
  }

  .managed-glow {
    position: absolute;
    top: -50%;
    left: 50%;
    transform: translateX(-50%);
    width: 120%;
    height: 100%;
    background: radial-gradient(ellipse at center, var(--secondary-glow) 0%, transparent 60%);
    pointer-events: none;
  }

  .managed-content {
    position: relative;
    z-index: 1;
  }

  .managed-badge {
    display: inline-block;
    font-family: var(--font-mono);
    font-size: 0.7rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    color: var(--secondary);
    background: var(--secondary-glow);
    padding: 0.35rem 0.85rem;
    border-radius: 6px;
    margin-bottom: 1.5rem;
  }

  .managed-title {
    font-family: var(--font-display);
    font-size: clamp(1.75rem, 3.5vw, 2.5rem);
    line-height: 1.2;
    letter-spacing: -0.02em;
    margin-bottom: 1.25rem;
  }

  .managed-desc {
    color: var(--text-secondary);
    font-size: 1.05rem;
    line-height: 1.7;
    max-width: 520px;
    margin: 0 auto 2rem;
  }

  .managed-highlight {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    text-align: left;
    background: rgba(139, 92, 246, 0.06);
    border: 1px solid rgba(139, 92, 246, 0.15);
    border-radius: 14px;
    padding: 1.25rem 1.5rem;
    margin-bottom: 2rem;
  }

  .highlight-icon {
    flex-shrink: 0;
    color: var(--secondary);
    margin-top: 0.15rem;
  }

  .managed-highlight p {
    color: var(--text-secondary);
    font-size: 0.9rem;
    line-height: 1.65;
  }

  .managed-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.5rem;
    background: var(--secondary-dim);
    color: white;
    text-decoration: none;
    border-radius: 12px;
    font-weight: 600;
    font-size: 0.9rem;
    transition: all 0.3s;
    box-shadow: 0 0 30px var(--secondary-glow);
  }

  .managed-cta:hover {
    transform: translateY(-2px);
    box-shadow: 0 0 50px rgba(139, 92, 246, 0.3);
  }

  @media (max-width: 600px) {
    .managed {
      padding: 2rem 0 5rem;
    }

    .managed-card {
      padding: 2.5rem 1.5rem;
    }
  }
</style>
